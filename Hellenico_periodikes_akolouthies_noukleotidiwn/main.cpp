#include <cstdio>
using namespace std;

const long MAXN=1005;
int N, K;
char str[MAXN];

int main() {
    freopen("dna.in", "r", stdin);
    freopen("dna.out", "w", stdout);
    scanf("%d %d", &N, &K);
    for(int i=0;i<N+1;i++) scanf("%c", &str[i]);

    return 0;
}