#include <bits/stdc++.h>
using namespace std;
int N,K;

int main() {

    FILE *fi=fopen("input","r");
    FILE *fo=fopen("output","w");
    fscanf(fi,"%d %d", &N, &K);

    fclose(fi);
    fclose(fo);

    return 0;
}